import Card from "../UIElements/Card";

import classes from "./NewComment.module.css";

const NewComment = (props) => {
  return <Card className={classes["new-comment"]}></Card>;
};

export default NewComment;
